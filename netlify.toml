[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "public"

# API запросы идут в функцию
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true