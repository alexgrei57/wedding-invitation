[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "public"

# API запросы идут в функцию
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Статические файлы (HTML, CSS, JS, картинки) обслуживаются напрямую
# Без редиректа на функцию!